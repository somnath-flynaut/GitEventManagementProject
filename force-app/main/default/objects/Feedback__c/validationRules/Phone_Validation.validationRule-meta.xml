<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Phone_Validation</fullName>
    <active>true</active>
    <description>To enforce a 10-digit phone number format on the Phone__c field.</description>
    <errorConditionFormula>NOT(REGEX(Phone__c, &quot;\\d{10}&quot;))</errorConditionFormula>
    <errorDisplayField>Phone__c</errorDisplayField>
    <errorMessage>Phone number must be exactly 10 digits</errorMessage>
</ValidationRule>
